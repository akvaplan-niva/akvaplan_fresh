// const save = async ({ key, property, children }) => {
//   console.warn(key, propery);
// };
// export default function Editable({ children, key, property }) {
//   return (
//     <div contenteditable onInput={() => save({ key, property, children })}>
//       {children}
//     </div>
//   );
// }
